# Community Pack Staging

Create one subdirectory per proposed community pack:

```text
community_pool/packs/<pack_name>/
```

Each pack should follow `../pack_schema.example.yaml`, and each candidate should follow `../candidate_schema.example.json`.

Do not commit large raw datasets, upstream repository mirrors, generated archives, or private material here. Use release assets or manifest entries for large files.
